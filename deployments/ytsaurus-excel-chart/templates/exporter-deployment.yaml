apiVersion: apps/v1
kind: Deployment
metadata:
  name: "ytsaurus-excel-exporter"
  labels:
    {{- include "ytsaurus-excel-chart.exporterLabels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  {{- if and .Values.settings.exporter.eventLog.enabled .Values.settings.exporter.eventLog.persistence.enabled }}
  # A single ReadWriteOnce event-log PVC can't be shared by an old and a new
  # pod during a rolling update (PVC attach conflict across nodes, and a
  # timbertruck pid-file lock conflict on the same node), so recreate instead.
  strategy:
    type: Recreate
  {{- end }}
  selector:
    matchLabels:
      {{- include "ytsaurus-excel-chart.exporterSelectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "ytsaurus-excel-chart.exporterLabels" . | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          command:
            - "/usr/bin/excel-exporter"
            - "-config"
            - "/etc/config.yaml"
            - "-log-to-stderr"
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.exporter.repository }}:{{ .Values.image.exporter.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.exporter.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.settings.exporter.http_port }}
              protocol: TCP
            - name: metrics
              containerPort: {{ .Values.settings.exporter.debug_http_port }}
              protocol: TCP
          livenessProbe:
            httpGet:
            {{- $apiPrefix := .Values.settings.exporter.api_path_prefix | default "" | trimAll "/" }}
            {{- with (first (.Values.settings.exporter.clusters | default .Values.settings.clusters)) }}
              path: {{ if $apiPrefix }}/{{ $apiPrefix }}{{ end }}/{{- .api_endpoint_name -}}/api/ready
            {{- end }}
              port: {{ .Values.settings.exporter.http_port }}

          readinessProbe:
            httpGet:
            {{- $apiPrefix := .Values.settings.exporter.api_path_prefix | default "" | trimAll "/" }}
            {{- with (first (.Values.settings.exporter.clusters | default .Values.settings.clusters)) }}
              path: {{ if $apiPrefix }}/{{ $apiPrefix }}{{ end }}/{{- .api_endpoint_name -}}/api/ready
            {{- end }}
              port: {{ .Values.settings.exporter.http_port }}
          volumeMounts:
            - mountPath: /etc/config.yaml
              name: config
              readOnly: true
              subPath: exporter-config.yaml
            {{- if .Values.settings.exporter.eventLog.enabled }}
            - mountPath: {{ .Values.settings.exporter.eventLog.logDir }}
              name: event-log
            {{- end }}
        {{- if and .Values.settings.exporter.eventLog.enabled .Values.settings.exporter.eventLog.timbertruck.enabled }}
        - name: timbertruck
          image: {{ .Values.settings.exporter.eventLog.timbertruck.image | required "settings.exporter.eventLog.timbertruck.image is required when timbertruck is enabled" | quote }}
          imagePullPolicy: IfNotPresent
          command:
            - "/usr/bin/timbertruck_os"
            - "-config"
            - "/etc/timbertruck/config.yaml"
          env:
            - name: {{ .Values.settings.exporter.eventLog.timbertruck.tokenSecretKey }}
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.settings.exporter.eventLog.timbertruck.tokenSecretName | required "settings.exporter.eventLog.timbertruck.tokenSecretName is required when timbertruck is enabled" | quote }}
                  key: {{ .Values.settings.exporter.eventLog.timbertruck.tokenSecretKey }}
            - name: YT_PROXY
              value: {{ .Values.settings.exporter.eventLog.timbertruck.ytProxy | quote }}
          volumeMounts:
            - mountPath: {{ .Values.settings.exporter.eventLog.logDir }}
              name: event-log
            - mountPath: /etc/timbertruck
              name: timbertruck-config
              readOnly: true
        {{- end }}
      volumes:
        - configMap:
            defaultMode: 420
            name: "ytsaurus-excel-cm"
          name: config
        {{- if .Values.settings.exporter.eventLog.enabled }}
        - name: event-log
          {{- if .Values.settings.exporter.eventLog.persistence.enabled }}
          persistentVolumeClaim:
            claimName: ytsaurus-excel-exporter-event-log
          {{- else if .Values.settings.exporter.eventLog.volume }}
          {{- toYaml .Values.settings.exporter.eventLog.volume | nindent 10 }}
          {{- else }}
          {{- fail "settings.exporter.eventLog: either persistence.enabled or volume must be set when eventLog.enabled is true" }}
          {{- end }}
        {{- end }}
        {{- if and .Values.settings.exporter.eventLog.enabled .Values.settings.exporter.eventLog.timbertruck.enabled }}
        - name: timbertruck-config
          configMap:
            defaultMode: 420
            name: ytsaurus-excel-exporter-timbertruck-config
        {{- end }}

      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
